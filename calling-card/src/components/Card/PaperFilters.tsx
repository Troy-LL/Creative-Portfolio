/** Filters unused for stock lighting — height map is canvas-lit. Kept for API. */
export function PaperFilters(_props: { seed: number }) {
  return null;
}

export function filterIds(_seed: number) {
  return { fine: "", soft: "" };
}
