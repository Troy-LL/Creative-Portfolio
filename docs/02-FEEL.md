# 02 — Feel (workbench)

Locked 2026-09-12 by Troy.

Current workbench after the `01` card/hatch. Not a museum. Not `Welcome.` as the product. `03` is parked.

## Floor

Paper `#F2EEE8` + light grain. Warm black ink. Bone floor continues from the door. Switzer labels. Boska only if a scrap wordmark appears. No new display face. No neon.

## Frames

3 pads max. Tinig · EditLayer · third empty (intentional).

Furniture trays/bezels. Live or sandbox UI inside only when peek is earned.

- **Tinig:** peek only when a clip exists; else dormant.
- **EditLayer:** peek when the editor runs; else quiet plate.

Never fake demo, store tile, or twin MacBooks.

## Axis (do not drift)

Seats in a **horizontal row**. **Input** = vertical scroll (wheel/trackpad down), same as `01`→`02`. **Visual** = horizontal pan (`translateX` only). Neighbor frames peek at edges. Not Abelarde’s vertical page. Not sideways-scroll as the input.

## Peek / leave

Enter = nested in-frame. Leave = pull back to desk. External link only if explicit. No full-bleed case-study takeover.

## Air / refuse

Third pad empty until something earns it. If it looks busy, cut.

Refuse: grid, Mac OS revival, Brittany, Bruno toybox, badge walls, skills chips, awards receipts, `Welcome.` as the `02` product.

## Chrome

Same stock as the calling card. Plaque/edge at most (`Work` / none). Motion: pan + soft settle; honor reduced-motion.

## Implement

No live implement until Troy names a sprint.
